{
  "name": "@space-pixels/aether",
  "version": "1.1.2",
  "main": "index.js",
  "license": "MIT",
  "engines": {
    "node": ">=v15.5.1"
  },
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "serve": "lerna run serve --stream",
    "prepack": "lerna run prepack --include-dependencies",
    "build": "lerna run build --include-dependencies",
    "clean": "lerna run clean",
    "packages:link": "lerna exec -- yarn link",
    "packages:unlink": "lerna exec -- yarn unlink"
  },
  "devDependencies": {
    "@types/node": "*",
    "lerna": "^4.0.0",
    "rimraf": "^3.0.2",
    "ts-node": "^10.4.0",
    "tsconfig-paths": "^3.11.0",
    "typescript": "^4.0.0"
  }
}
