{
  "name": "@space-pixels/aether-client",
  "version": "1.1.3",
  "main": "build/index.js",
  "types": "build/index.d.ts",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "ssh://git@github.com/space-pixels/aether.git",
    "directory": "client"
  },
  "engines": {
    "node": ">=v15.5.1"
  },
  "scripts": {
    "serve": "tsc -p tsconfig.build.json -w",
    "prepack": "yarn clean && yarn build",
    "build": "tsc -p tsconfig.build.json",
    "clean": "rimraf -rf ./build"
  },
  "dependencies": {
    "@space-pixels/aether-core": "^1.1.3",
    "protobufjs": "^6.0.0",
    "rxjs": "^7.0.0",
    "uuid": "^8.0.0"
  },
  "devDependencies": {
    "@types/node": "^16.11.4",
    "@types/uuid": "^8.0.0",
    "typescript": "^4.0.0"
  }
}
