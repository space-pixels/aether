{
  "name": "@space-pixels/aether-server",
  "version": "1.1.2",
  "main": "build/index.js",
  "types": "build/index.d.ts",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "ssh://git@github.com/space-pixels/aether.git",
    "directory": "server"
  },
  "engines": {
    "node": ">=v15.5.1"
  },
  "scripts": {
    "serve": "tsc -p tsconfig.build.json -w",
    "prepack": "yarn clean && yarn build",
    "build": "tsc -p tsconfig.build.json",
    "clean": "rimraf -rf ./build"
  },
  "dependencies": {
    "@space-pixels/aether-core": "*",
    "protobufjs": "^6.0.0"
  },
  "devDependencies": {
    "@types/node": "^16.11.4",
    "typescript": "^4.0.0"
  }
}
