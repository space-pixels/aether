{
  "name": "@space-pixels/aether-adapter-direct",
  "version": "1.1.3",
  "main": "build/index.js",
  "types": "build/index.d.ts",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "ssh://git@github.com/space-pixels/aether.git",
    "directory": "adapters/direct"
  },
  "engines": {
    "node": ">=v15.5.1"
  },
  "scripts": {
    "serve": "tsc -p tsconfig.build.json -w",
    "prepack": "yarn clean && yarn build",
    "build": "tsc -p tsconfig.build.json",
    "clean": "rimraf -rf ./build"
  },
  "dependencies": {
    "protobufjs": "^6.0.0"
  },
  "devDependencies": {
    "@types/node": "^16.11.4",
    "typescript": "^4.0.0"
  }
}
